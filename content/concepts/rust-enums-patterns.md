---
id: concept.rust-enums-patterns
title: Rust Enums and Pattern Matching
slug: rust-enums-and-pattern-matching
topic: topic.rust-fundamentals
description: Algebraic data types and exhaustive control flow with match expressions.
---
# Rust Enums and Pattern Matching

Rust enums model a value as one of several variants, each of which can carry different data. `match` handles variants exhaustively, making unhandled states visible at compile time.
