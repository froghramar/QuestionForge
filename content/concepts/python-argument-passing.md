---
id: concept.python-argument-passing
title: Python Argument Passing
slug: python-argument-passing
topic: topic.python-fundamentals
description: Object references, rebinding, and mutation when values are passed to functions.
---
# Python Argument Passing

Python passes object references to functions. A function can mutate a passed mutable object, but rebinding its local parameter does not change the caller's variable binding.
